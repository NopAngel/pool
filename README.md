### Pool Linux

Clean your Linux distro 🐧

With *Pool Linux* you can optimize any 
Linux distribution (Debian, Arch, OpenSUSE, Fedora, and Linux in general). 

Easy and simple to run!!!


### How to run?

To run *Pool Linux* you need to use these commands:

```bash
git clone https://github.com/NopAngel/pool.git && cd pool
chmod +x ./run.sh
./run.sh
```

### LICENSE

- The license is *MIT* (Read the `LICENSE.txt` file)
